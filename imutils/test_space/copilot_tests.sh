# write a function to multiply two numbers
def multiply(a, b):
    """ Multiply two numbers and return the result"""
    return a * b

# make a scatter plot of a dataframe, variable x is signal and variable y is distance
combined.iloc[initial,end].plot.scatter(x='signal', y='distance')